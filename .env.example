# =============================================================================
# Pilot Marketing Site — Environment Variables
# =============================================================================
# V1: No environment variables required.
# V2: Uncomment and fill in when connecting to the Pilot backend.
# =============================================================================

# Pilot API base URL (server-side, not exposed to browser)
# PILOT_API_URL=https://api.yourdomain.com

# Pilot Tenant ID (public — identifies which tenant's content to display)
# NEXT_PUBLIC_PILOT_TENANT_ID=

# MCP Token for the Research Desk (CONSUMER role JWT)
# Generated via: POST /api/v1/auth/signup with role=CONSUMER
# NEXT_PUBLIC_MCP_TOKEN=

# Site URL (used for OG tags and canonical URLs)
# NEXT_PUBLIC_SITE_URL=https://pilotcms.com
